<% include "prefix.tex" %>
\begin{document}
<% if has_frontmatter %>
\frontmatter
<% endif %>
<% if titlepage %>
<% include "title.tex" %>
<% endif %>
<% if cover %>
<% include "cover.tex" %>
<% endif %>
<% for item in include_before %>
<< item >>
<% endfor %>
<% if toc %>
  <% if toc_title %>
\renewcommand*\contentsname{<< toc_title >>}
  <% endif %>
{
\setcounter{tocdepth}{<< toc_depth >>}
\tableofcontents
}
<% endif %>
<% if lof %>
\listoffigures
<% endif %>
<% if lot %>
\listoftables
<% endif %>
<% if linestretch %>
\setstretch{<< linestretch >>}
<% endif %>
<% if has_frontmatter %>
\mainmatter
<% endif %>
<% include body %>
<% if has_frontmatter %>
\backmatter
<% endif %>
<% if natbib %>
  <% if bibliography %>
    <% if biblio_title %>
      <% if biblio_title.has_chapters %>
\renewcommand\bibname{<< biblio_title >>}
      <% else %>
\renewcommand\refname{<< biblio_title >>}
      <% endif %>
    <% endif %>
  \bibliography{<< bibliography | listify | join(",") >>}

  <% endif %>
<% endif %>
<% if biblatex %>
\printbibliography<% if biblio_title %>[title=<< biblio_title >>]<% endif %>

<% endif %>
<% for item in include_after %>
<< item >>
<% endfor %>
\end{document}
